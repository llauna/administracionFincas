export interface Empresa {
    _id: string;
    nombre: string;
    // Agrega aquí otros campos que tenga tu modelo de Empresa
    direccion?: string;
    telefono?: string;
    email?: string;
    createdAt?: string;
    updatedAt?: string;
}